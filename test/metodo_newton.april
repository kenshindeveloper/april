fn sqr(x:int) {
    z := 1.0;
    cont := 0;

    for cont < 10 {
        z = z-(((z*z)-x)/(2*z));
        println("cont: "+str(cont));
        cont += 1;
    }
    return z;
}

fn main()
{
    sqr(5);
    j := sqr(2);
    println("result: "+str(j));
}

main();